namespace cdm.event.smartcontract: <"Defines enumerations used to classify and orchestrate Smart Contract types.">
version "${project.version}"

enum SmartContractTypeEnum: <"The enumerated values to specify the type of smart contract.">
    ResetInstruction <"The relevant Steps for this Smart Contract are: collectFloatingRateOptionIndex, recordFloatingRateOptionData, calculateObservationPeriod, adjustDates, recordMarketRates, and calculateResetValue.">